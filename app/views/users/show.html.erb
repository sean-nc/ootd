<h2><%= @user.username %></h2>
<%= render 'shared/stats' %>
<%= render 'follow_form' if user_signed_in? %>

<% @posts.each do |p| %>
  <%= link_to p.id, p %>
  <%= p.content %>
<% end %>

<% if current_user?(@user) %>
  <%= link_to "New Post", new_post_path %>
<% end %>